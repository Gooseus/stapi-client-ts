/**
 * Header book series, embedded in other objects
 */
export type BookSeriesHeader = {
    /**
     * Book series unique ID
     */
    uid?: string;
    /**
     * Book series title
     */
    title?: string;
};
