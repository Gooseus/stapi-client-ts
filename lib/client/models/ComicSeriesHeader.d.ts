/**
 * Header book series, embedded in other objects
 */
export type ComicSeriesHeader = {
    /**
     * Comic series unique ID
     */
    uid: string;
    /**
     * Comic series title
     */
    title: string;
};
