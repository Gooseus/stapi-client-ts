/**
 * Header conflict, embedded in other objects
 */
export type ConflictHeader = {
    /**
     * Conflict unique ID
     */
    uid: string;
    /**
     * Conflict name
     */
    name: string;
};
